# Guardfile is no longer configured for Minitest.
#
# If you want to use Guard with RSpec, add guard-rspec to your Gemfile and
# configure a guard :rspec block here.
#
# Example:
#   guard :rspec, all_on_start: false do
#     watch('spec/spec_helper.rb')
#     watch('spec/rails_helper.rb')
#     watch(%r{^spec/.+_spec\.rb$})
#     watch(%r{^app/(.+)\.rb$}) { |m| "spec/#{m[1]}_spec.rb" }
#   end
